# Land Recovery Monitoring

Prototype React + Vite for the Land Recovery Monitoring dashboard.

## Requirements
- Node.js LTS
- VS Code

## Run
```bash
npm install
npm run dev
```

Then open the local URL shown by Vite, normally:
http://localhost:5173

This prototype uses mock data only. Firebase will be added after the UI/UX and workflow are approved.
